---
import { Image } from 'astro:assets';
import CardMain from './CardMain.astro';

const data = [
	{
		id: 1,
		text: 'a Spanish-speaking creator of a business or project',
	},
	{
		id: 2,
		text: 'ready to transform your venture into English',
	},
	{
		id: 3,
		text: 'eager to have an even greater positive impact in the world',
	},
];
---

<section class="relative py-24 bg-white">
	<!-- <Image
		src={curvedArrow}
		alt="descriptive text"
		width="140"
		height="150"
		class="absolute left-1/2 -translate-x-1/2 -top-12 rotate-30 mx-auto"
		loading="eager"
	/> -->

	<h2
		class="text-2xl text-center font-OpenSans mb-6 md:text-4xl md:mb-12 max-w-[480px] mx-auto"
	>
		You’re in <span class="text-darkGreen">THE PERFECT</span> place if you are...
	</h2>
	<div
		class="grid sm:grid-cols-2 md:grid-cols-[repeat(auto-fit,minmax(250px,1fr))] max-w-[800px] justify-center mx-auto gap-4"
	>
		{data.map((item) => <CardMain id={item.id} text={item.text} />)}
	</div>
</section>
